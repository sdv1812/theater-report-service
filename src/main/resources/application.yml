cloud.aws:
  region: ap-southeast-2
  reportS3Bucket: theater-dev
  reportDynamoDb: theater-premium-report-dev
server:
  port: 8081
spring:
  datasource:
    url: jdbc:h2:file:~/theater;AUTO_SERVER=TRUE
    driverClassName: org.h2.Driver
    username: sa
  jpa:
    properties:
      hibernate:
        hbm2ddl:
          auto: update

---
spring:
  config:
    activate:
      on-profile: dev
  datasource:
    url: jdbc:h2:file:~/theater;AUTO_SERVER=TRUE
    driverClassName: org.h2.Driver
    username: sa
cloud.aws:
  reportS3Bucket: theater-dev
  reportDynamoDb: theater-premium-report-dev

---
spring:
  config:
    activate:
      on-profile: staging
  datasource:
    url: jdbc:h2:file:~/theater;AUTO_SERVER=TRUE
    driverClassName: org.h2.Driver
    username: sa
cloud.aws:
  reportS3Bucket: theater-stg
  reportDynamoDb: theater-premium-report-stg

---
spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url: jdbc:h2:file:~/theater;AUTO_SERVER=TRUE
    driverClassName: org.h2.Driver
    username: sa
cloud.aws:
  reportS3Bucket: theater-prod
  reportDynamoDb: theater-premium-report-stg